# Environment
ENV=development
DEBUG=true

# Backend
SECRET_KEY=dev-secret-change-me
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
ALLOWED_HOSTS=*
# CORS_ORIGINS: comma-separated list (used by backend CORS config)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Postgres (used by the Postgres container)
POSTGRES_USER=dev_user
POSTGRES_PASSWORD=dev_password
POSTGRES_DB=dev_db
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Database URL used by the backend (adjust scheme if using async driver)
DATABASE_URL=postgresql://dev_user:dev_password@db:5432/dev_db

# Frontend (Vite expects env vars prefixed with VITE_)
# For local dev the browser typically hits localhost:8000 for the API (compose maps ports)
VITE_API_URL=http://localhost:8000
